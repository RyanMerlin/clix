# Consolidated into ci.yml — this workflow no longer runs automatically.
name: build-linux (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replaced by ci.yml. Use that workflow for all CI."
