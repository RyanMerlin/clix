name: gcloud.aiplatform.endpoints.list
version: 1
description: "List Vertex AI endpoints in a GCP project"
backend:
  type: subprocess
  command: gcloud
  args:
    - ai
    - endpoints
    - list
    - "--project={{ inputs.project }}"
    - "--region={{ inputs.region }}"
    - "--format=json"
risk: low
sideEffectClass: read-only
inputSchema:
  type: object
  required: [project, region]
  properties:
    project:
      type: string
      description: "GCP project ID"
    region:
      type: string
      description: "GCP region (e.g. us-central1)"
