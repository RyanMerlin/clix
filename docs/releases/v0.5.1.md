# clix v0.5.1

This release refreshes the public-facing documentation so it matches the current
`main` branch after the `v0.5.0` release.

## Highlights

- Updated the README to point at the current release and release notes.
- Refreshed the release guide to reference the latest tag.
- Kept the codebase and release process aligned for public consumption.

## Verification

The workspace was previously validated with:

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-features`

## Notes

This is a documentation-only release from the repository's current `main` branch.
